# 🔒 安全策略

## 1. 安全承诺

本项目以**本地优先、最小暴露**为安全原则：

- 密钥（`LLM_API_KEY`）只存于 `.env`（已 gitignore），**绝不入库、不写入日志**；
- 上传文件限制类型与大小，文件名清洗防路径穿越；
- 前端 Markdown 渲染**关闭 HTML**，防存储型 XSS；
- 默认仅在本地 `0.0.0.0:8002` 提供服务，生产建议放内网或加认证。

## 2. 支持的版本

| 版本 | 支持状态 |
|------|----------|
| 3.x（当前主线） | ✅ 受支持，建议保持最新 |
| 2.x 及更早（Flask 版） | ❌ 不再维护 |

## 3. 报告漏洞

如果你发现安全问题，**请不要公开发布 Issue**，请按以下方式私下报告：

- 邮箱：*（维护者在此填写联系方式）*
- 或联系本仓库维护者

请在报告中说明：

1. 问题描述与影响面；
2. 复现步骤（如需要）；
3. 建议的修复方案（可选）。

我们会尽快（通常 3 个工作日）响应，并在修复后统一致谢。

## 4. 已知的安全注意事项（部署前必读）

- **不要**把 `.env` 或其中的密钥上传到公开仓库或 CI 日志；
- **生产环境**建议收紧 `CORS_ORIGINS`（默认 `*`，见 `backend/core/config.py`）；
- 若暴露到公网，建议在 Nginx 层加 HTTPS 与访问控制（见 [部署运维指南](./docs/部署运维指南.md)）；
- 请勿上传**敏感个人信息**到知识库——上传内容仅用于本地检索与回答生成。
